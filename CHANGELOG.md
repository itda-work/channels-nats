# Changelog

Keep a Changelog 형식. subject 규약이 바뀌면 여기와 README에 남기고 메이저(1.0 전에는 마이너)를 올린다.

## [Unreleased]

### Changed

- **"daphne 종료가 멈춘다"는 서술을 내렸다.** 0.7.0이 되살리는 그 취소가 종료 정지로 이어진다고 README에 적었는데, **실물 daphne로는 재현되지 않았다** — 최소 Channels 앱을 daphne로 띄우고 실제 websocket으로 붙어, 브로커를 세워 컨슈머를 `group_send` 안에 수 초간 붙들어 둔 상태로 SIGTERM을 보냈다. 취소는 `CancelledError`로 정상 전파되고 daphne는 0.1~0.2초에 종료했다 — 가드가 없는 0.6.1에서도 같았다(연결 1개·2개, 버스트·지속 포화 네 조합). 삼킴 자체는 격리된 조건에서 여전히 재현되므로 가드는 그대로 두고, 그 결과에 대한 주장만 사실에 맞췄다 (#23)

## [0.7.1] - 2026-09-10

### Added

- **닫힌 루프가 연결을 들고 갔다는 것을 경고한다.** 연결은 그것을 연 루프의 것이라 루프가 닫히면 레이어가 닫아 줄 수 없다 — `writer.close()`는 "Event loop is closed"로 실패하고 `transport.abort()`는 실행되지 않을 작업을 예약할 뿐이다(둘 다 실측). 가비지 컬렉터가 거둘 때까지 서버 쪽 연결이 남는다. 실측: `async_to_sync(layer.group_send)` **20회에 연결 17개**(gc 후 2개). 기존 `loop_state_warn_at` 경고는 이 경우를 못 잡는다 — 그 상태들은 정리되므로 세는 수가 늘지 않는다. 이제 닫힌 루프가 살아 있는 연결을 남긴 횟수를 세어 `drop_log_interval` 간격으로 경고하고, README에 실측값과 피하는 법을 적었다

### Fixed

- **`flush()`가 아직 구독 중이던 `receive()`를 멈춰 세웠다.** `_mailbox()`는 구독하기 **전에** box를 등록하므로, 그 사이 `flush()`가 오면 box를 superseded로 표시한다 — 그런데 아직 수신자가 0이라 깨울 항목을 넣지 않고, `_mailbox()`는 그 box를 그대로 돌려준다. 그 읽기는 아무도 채우지 않는 queue에서 기다렸고 `close()`도 닿지 못했다(box가 이미 `mailboxes`에서 빠져서). #21·#22와 같은 멈춤이 한 창 앞에서 일어난 것이다. 이제 `receive()`가 **기다리기 전에** 두 플래그를 본다 (외부 검토에서 발견)
- **`flush()`가 앞지른 `group_add`가 구독만 남겼다.** `_group_subscribe()`는 서버가 구독을 받았는지 확인한 뒤 멤버십이 아직 있는지 **다시 보지 않고** 기록했다. 그 사이 `flush()`가 멤버십을 지우면 **멤버 없는 그룹 구독**이 남아 그룹의 모든 트래픽을 받아 빈 멤버 목록에 대고 버렸다(서버 구독으로 확인). `_resubscribe()`는 같은 이유로 이미 재검사를 한다. 이제 최초 생성 경로도 한다 (외부 검토에서 발견)
- **가득 찬 mailbox의 드롭 비용.** 만료 회수는 queue를 통째로 비웠다 다시 채우므로, capacity를 넘겨 도착하는 **메시지마다** 전체 비용을 냈다 — 실측 capacity 50,000에서 드롭당 **8.673 ms**(같은 이벤트 루프의 다른 일이 그만큼 밀린다). 이제 가장 오래된 도착 시각을 들고 있다가 **회수할 것이 없으면 순회를 건너뛴다**: 같은 조건에서 드롭당 **0.044 ms**. 표시는 실제보다 오래된 쪽으로만 틀릴 수 있어(`receive()`가 앞에서 꺼내므로) 헛도는 순회 한 번을 낼 뿐 회수할 것이 있는 순회를 건너뛰지는 않는다 (외부 검토에서 발견)
- **읽을 수 없는 프레임 경고가 원인을 단정했다.** "Something other than this layer is publishing to its subjects"라고 적었는데, **이 레이어가 보낸 메시지도 그렇게 될 수 있다** — msgpack은 문자열이 아닌 map key를 packb는 받아 주고 unpackb는 거부한다(실측: `{1: 'x'}`가 중첩되면 `send()`는 성공하고 `receive()`가 버린다). 진단하는 사람을 없는 발행자를 찾으러 보내는 문구라, 사실만 적고 두 가능성을 나란히 두도록 고쳤다 (외부 검토에서 발견)
- **그룹의 마지막 프로세스 채널이 빠져도 그룹 구독이 남았다.** 그룹 구독은 이 프로세스의 **프로세스 전용 채널**들을 위한 것이고, 그룹에 속한 일반 채널은 자기 큐 구독으로 받는다. 그런데 `group_discard()`는 **그룹이 통째로 빌 때만** 그룹 구독을 해지해서, 일반 채널 멤버가 남아 있으면 그 구독이 계속 살아 있었다(서버의 `/subsz`로 확인: 큐 없는 구독이 그대로 남는다). 그 그룹에 발행되는 모든 메시지가 이 연결로 계속 와서 콜백이 전부 버렸다 — 대역폭과 CPU를 쓰고, nats-py의 pending 한도(#18의 그 한도)까지 갉아먹는다. 이제 `!`가 있는 멤버가 하나도 남지 않으면 해지한다

- **취소 뒤 정리가 연결이 풀릴 때까지 끝나지 않았다.** 두 자리다. (1) `_subscribe()`는 취소가 핸들을 잃지 않도록 `Client.subscribe()`를 자식 태스크로 띄우고 나가는 길에 그것을 기다려 해지하는데(#11), 그 기다림에 끝이 없었다. (2) `Subscription.unsubscribe()`도 명령을 보내므로 같은 자리에서 기다린다. 송신 backpressure로 연결이 막히면 둘 다 연결이 풀릴 때까지 반환하지 않아서, **취소된 호출자가 그때까지 살아 있었다** — 실제 서버로 재현했다: 한 번 취소 뒤 5초가 지나도 태스크가 살아 있었고(구독 생성), 연결을 계속 포화시키면 8초가 지나도 파킹 상태였다(해지). daphne처럼 한 번 취소하고 `gather`로 기다리는 종료 경로에서는 #23과 같은 정지가 된다.

  이제 둘 다 `CLEANUP_GRACE`(1초)까지만 기다리고, 그 안에 안 끝나면 **백그라운드 태스크에 넘긴다** — 호출자는 즉시 끝나고 명령은 나중에라도 나간다. 그 태스크들은 루프 상태가 들고 있어 `close()`가 취소한다(루프가 닫힐 때 pending 태스크로 남지 않는다). 취소가 그 명령 자체를 취소하지 않도록 `shield`로 감쌌다 — 취소해 버리면 구독이 살아남아 자기 큐 그룹의 메시지를 계속 가져간다(#11이 고친 바로 그 상태다) (#26)

- **`close()`가 막힌 연결에서 예외를 던졌다.** drain은 대기 중인 것을 내보내는 일이라 서버가 읽지 못하면 끝나지 않고, nats-py가 자기 `drain_timeout`(기본 30초)·`flush_timeout`(기본 10초)을 다 쓴 뒤 `FlushTimeoutError`를 올렸다 — 그것이 `close()` 밖으로 나갔다(실제 서버로 재현). 종료 경로에서 레이어를 닫는 코드가 예외를 받는다는 뜻이다. 이제 그 실패를 경고로 남기고 연결을 직접 닫는다. 직접 닫는 것도 막힌 소켓을 기다리므로(`_drain_helper`에서 무한 대기하는 것을 확인했다) `CLEANUP_GRACE`로 유계다. **걸리는 시간 자체는 nats-py의 drain 의미론이 정한다** — 실측 약 50초(기본값), `connect_options`로 두 타임아웃을 낮추면 약 14초 (#26)

- **`close()`가 자기가 취소한 것을 정착시키지 않고 반환했다.** 취소된 태스크는 루프가 한 번 돌기 전까지 끝난 것이 아니다. `close()`는 재접속 루프와 백그라운드 명령을 취소한 뒤 연결을 drain하는데, **재접속이 도는 상황은 바로 drain할 연결이 없는 상황**이라 양보가 일어나지 않고 그대로 반환했다(실제 서버를 죽여 재현: `close()` 직후 `recovery done: False`). `asyncio.run`은 남은 태스크를 자기가 거둬 주지만, 그냥 멈추는 루프(daphne의 Twisted reactor)에서는 "Task was destroyed but it is pending!"으로 남는다. 이제 취소한 것들을 `CLEANUP_GRACE`까지 기다렸다가 반환한다 — 취소를 받지 않는 것이 있어도 `close()`를 붙들지 못하게 유계다

- **백그라운드로 넘긴 명령이 닫은 루프의 상태를 되살릴 수 있었다.** 위 처방이 넘긴 태스크를 루프 상태에 보관하는데, 그 상태를 `_state()`로 가져오면 없을 때 새로 만든다 — `close()` 직후에 넘겨진 명령이 방금 닫은 루프의 상태를 되살렸다. #24와 같은 모양이고, 이 릴리스에서 새로 들어온 것이라 배포 전에 잡았다. 이제 상태가 없으면 넘기지 않고 그 명령을 취소한다 — 연결이 이미 갔으므로 닿을 곳이 없다

- **연결이 성립하지 못한 소켓이 GC 때까지 남았다.** `nats.connect()`는 클라이언트를 자기 안에서 만들므로 실패한 호출은 아무것도 돌려주지 않고, 그 사이 열린 소켓은 가비지 컬렉터가 거둘 때까지 상대편에 그대로 있었다 — TCP는 받아 주지만 INFO를 보내지 않는 서버(먹통 브로커, 백엔드 없는 로드밸런서)로 실측: **타임아웃 5회에 소켓 5개**. 재접속 루프는 레이어가 살아 있는 한 계속 재시도하므로 그만큼 쌓인다. 이제 레이어가 클라이언트를 직접 만들어 들고 있다가, 연결이 성립하지 못하면 닫는다. **취소된** connect는 여전히 남는다 — `close()`가 `is_closed`를 세우고도 소켓을 놓지 않으며 transport를 직접 닫아도 마찬가지다(실측). 취소 한 번에 소켓 하나이고, 쌓이던 쪽은 재시도였다

- **재접속 로그가 복원한 구독 수를 적게 셌다.** 그룹에 속한 **일반 채널**의 큐 구독(`group_channel_subscriptions`)이 집계에서 빠져 있었다 — 두 개를 복원하고도 "restored 1 subscriptions"이라고 적었다(실측). 재접속을 진단하는 운영자에게 잘못된 수를 주는 것이라 고쳤다. 그 네 번째 복원 경로에는 테스트가 없었고, 이제 있다 — 두 프로세스가 같은 이름을 읽는 상태에서 그룹 메시지가 **합쳐서 한 번만** 도착하는지를 재접속 전후로 확인한다(동작 자체는 전후 모두 옳았다)

## [0.7.0] - 2026-09-09

### Changed

- **`send()`·`group_send()`가 `asyncio.CancelledError`를 던질 수 있다.** nats-py가 삼킨 취소를 레이어가 되살리기 때문이다(아래 Fixed). 종료 경로에서만 보이는 변화이고, 취소를 요청한 쪽이 그것을 돌려받는 것이라 의미상 옳다
- **지원하는 Python 하한을 3.13으로 올린다** (3.10·3.11·3.12 지원 중단). `Task.cancelling()`이 3.11부터라 그 아래에서는 nats-py가 삼킨 취소를 레이어가 알아볼 방법이 없고, 버전마다 갈라지는 코드를 두느니 하한을 올리는 쪽을 택했다. Django 4.2 LTS는 2026년 4월에 EOL이고 현행 LTS인 5.2가 3.13을 지원하므로 지원되는 Django 조합은 끊기지 않는다. 3.12 이하가 필요하면 0.6.1에 고정한다. CI 매트릭스도 ubuntu(3.13·3.14) + windows-latest(3.13)로 줄었다 — 이제 **Windows가 하한 버전을 돈다**

### Fixed

- **nats-py가 삼킨 취소가 컨슈머를 계속 돌게 두었다.** `Client._flush_pending()`이 `except asyncio.CancelledError: pass`로 끝나, 송신 backpressure로 flush를 기다리는 동안 도착한 취소가 버려지고 `publish()`가 아무 일 없었다는 듯 반환한다(nats-py 2.15.0 기본 설정, 실제 서버로 재현). 그러면 종료 중 취소된 컨슈머가 자기 루프로 돌아가 끝나지 않고, **한 번만 취소하고 `gather`로 기다리는** daphne의 종료 경로가 멈춘다(daphne의 두 단계를 실제 Channels 컨슈머 위에서 재현). 이제 `_publish()`가 호출 전후의 `Task.cancelling()`을 비교해 그 취소를 되살린다 — nats-py는 `uncancel()`을 부르지 않으므로 요청이 세어진 채 남는 것이 유일한 흔적이다. 되살리는 것은 호출자의 취소이지 메시지가 아니며, **진짜 고칠 자리는 nats-py다** (#23)

## [0.6.1] - 2026-09-09

### Changed

- **CI가 Windows에서도 테스트를 돌린다** (`windows-latest`, Python 3.13). 지금까지 Windows 지원은 코드 원칙으로만 지켰고 실행된 적이 없었다. lint·타입 검사는 Linux에 남긴다 — 플랫폼과 무관하고, Windows 체크아웃은 CRLF라 포맷 검사가 헛돈다. 여전히 CI 밖인 것은 Windows에서의 3.10과 ARM Windows이며, 0.6.0을 ARM Win11에서 손으로 확인했다(3.13 arm64·x64, 63/63)

### Fixed

- **`close()` 뒤에 취소된 `receive()`가 닫은 루프의 상태를 되살렸다.** 일반 채널의 취소된 읽기는 mailbox를 놓아주려고 `_discard_mailbox()`를 부르고, 그것이 `_state()`로 상태를 **없으면 만들어** 가져왔다. `close()`가 상태를 버린 직후 대기자가 깨어나기 전에 취소가 도착하면 그 경로를 타서, 연결도 구독도 없는 빈 `_LoopState`가 되살아났다(실제 서버로 재현). 그 항목은 `loop_state_warn_at` 집계에 들어가므로, 루프를 여닫는 프로세스가 **연결을 붙들고 있지도 않은데** "N개의 이벤트 루프가 이 레이어를 썼다" 경고를 볼 수 있었다. 이제 정리 경로는 상태를 만들지 않고 조회만 한다 (#24)

## [0.6.0] - 2026-09-09

### Fixed

- **`close()`가 대기 중인 `receive()`를 영원히 멈춰 두었다.** `close()`는 그 루프의 상태를 통째로 버리고 연결을 drain하므로 이미 `queue.get()`에 걸려 있던 `receive()`는 버려진 mailbox를 쥔 채 남았고, 누군가 취소하기 전까지 아무 일도 일어나지 않았다(실제 서버로 재현, 0.5.0에서도 재현됨). 이제 `close()`가 그 대기자들을 깨우고 `receive()`는 새 예외 **`ChannelLayerClosed`**(`RuntimeError` 하위, `channels_nats`에서 export)로 끝난다. `flush()`의 처방(새 mailbox로 옮기기)은 쓸 수 없었다 — 새 mailbox를 얻으려면 방금 닫은 연결을 다시 열게 된다 (#22)

`receive()`의 계약이 바뀐다. 실제 Channels 컨슈머로 확인한 결과: 컨슈머 인스턴스가 이 예외로 끝나고, daphne의 정상 종료 경로(`kill_all_applications`)는 `addErrback`으로 삼켜 로그에 남기지 않는다. 서버가 살아 있는 동안 `close()`를 부르면 `application_checker`가 "Exception inside application"으로 남긴다.

## [0.5.0] - 2026-09-09

### Changed

- **`expiry`가 무엇을 재는지 명시한다.** 시각은 콜백이 메시지를 mailbox에 넣는 순간에 찍히므로 `expiry`는 **mailbox 체류 시간**이지 메시지의 나이가 아니다. 네트워크 지연도 nats-py의 pending 대기 시간도 세지 않는다. 발행 시각으로 옮기면 발행자의 시계를 믿어야 해서 그렇게 하지 않았다. 동작은 그대로이고, 특성화 테스트로 고정했다 (#5)
- **`capacity`가 채널 전체 이름별로 적용된다는 것이 스펙 MUST로부터의 의도적 이탈임을 명시한다.** 공유 적용하면 연결이 많은 서버에서 연결들이 서로의 대기열을 밀어낸다. 함께, mailbox 앞에 **공유 대기열이 하나 더 있다**는 사실을 적었다 — 한 프로세스 subject의 nats-py 구독 pending 대기열은 그 루프의 모든 전용 채널이 함께 쓴다. 동작 변화 없음 (#5)
- 클러스터 테스트가 검증하는 것이 **eventual reachability이지 최초 1회 발행의 무손실이 아님**을 테스트 docstring과 CLAUDE.md에 적었다. 노드 재시작 후 복귀 경로를 테스트로 고정했다 (#20)
- 취소가 nats-py의 `_flush_pending()` 안에서 삼켜져 사라질 수 있다는 것을 CLAUDE.md에 적었다. 레이어 상태는 일관되게 남고, 그 경로에서는 레이어가 감지할 수도 없다 (#19)

### Fixed

- **구독 생성 중 취소가 nats-py 안에 살아 있는 구독을 남겼다.** `Client.subscribe()`는 구독 등록과 콜백 태스크 생성을 마친 **뒤에** 와이어를 기다리므로, 그 await에서 취소되면 `Subscription`이 호출자에게 돌아오지 않는다 — 받지 못한 핸들은 해지할 수도 없다. 남은 구독은 계속 배달한다. 일반 채널은 큐 그룹으로 구독하므로 그 몫을 가로채 아무도 읽을 수 없는 mailbox에 넣고(실제 서버로 재현: 20건 중 일부만 도착), 프로세스·그룹 subject라면 같은 메시지를 두 번 넣는다. 이제 구독 생성을 태스크로 띄워 취소돼도 핸들이 남고, 나가는 길에 해지한다. 대기 중 두 번째 취소가 오면 여전히 남는다 — 기다릴 곳이 없다 (#11)
- **읽을 수 없는 프레임이 컨슈머를 끝냈다.** subject는 외부 계약이고 README가 다른 발행자를 초대하는데, msgpack map이 아닌 프레임이 오면 `receive()`가 예외를 밖으로 던졌다 — 잘못한 것이 없는 컨슈머가 종료되고, 그룹 subject에서는 잘못된 발행 하나가 그룹 전원을 끝낸다. 이제 만료된 메시지처럼 버리고 채널·이유·누적 개수를 경고한다(첫 건에 한 번, 이후 `drop_log_interval`에 한 번). 이 레이어로 보내는 쪽은 지금처럼 `send()`에서 dict인지 검사한다 (#16)
- **프로세스 prefix로 `receive`하면 아무것도 오지 않았다.** `_process_deliver()`가 `Channel` 헤더의 전체 이름으로만 mailbox를 찾아, 스펙이 프로세스를 가리킨다고 기술하는 `<name>.<id>!` 앞부분으로 만든 mailbox에는 아무것도 배달되지 않았다. 이제 전체 이름 mailbox가 없으면 prefix mailbox로 배달한다. 보통의 컨슈머는 `new_channel()`이 준 전체 이름을 쓰므로 동작 변화가 없다 (#15)
- **nats-py 단계에서 버려진 메시지가 어디에도 남지 않았다.** 구독이 nats-py의 pending 한도를 넘으면 메시지는 mailbox에 닿기 전에 버려지는데, 레이어는 자기 대기열이 넘칠 때만 세므로 운영자에게는 "레이어는 아무것도 버리지 않았다"로 보였다(실제 서버 재현: 161건 중 11건 유실, 레이어 집계 0). 이제 레이어가 `error_cb`를 감싸 `SlowConsumerError`를 `channels_nats` 로거에 subject·누적 개수와 함께 경고한다 — 첫 건에 한 번, 이후 `drop_log_interval`에 한 번으로 mailbox 드롭 경고와 같은 방식이다. 사용자가 준 `error_cb`는 그대로 호출되고, 주지 않은 경우 다른 오류는 지금까지처럼 `nats` 로거에 남는다. mailbox별 `dropped`에 합치지는 않는다 — 손실 단위가 구독이고, 프로세스 subject 하나가 그 프로세스의 모든 채널을 나른다 (#18)
- `flush()`가 **대기 중인 `receive()`를 죽은 queue에 남겼다.** mailbox 사전만 비우므로, 이미 기다리던 수신자는 다시는 채워지지 않는 옛 queue에서 기다리고 새 메시지는 새 mailbox에 쌓였다 — 취소되기 전까지 조용히 멈춘다(실제 서버로 재현). 이제 `flush()`가 버리는 mailbox의 대기자를 깨우고, 깨어난 `receive()`는 그 채널의 새 mailbox로 옮겨 계속 기다린다. `receive()`의 계약(메시지를 돌려준다)은 그대로다 (#21)
- 프로세스 구독을 **flush 전에 기록했다.** 그 flush에서 취소되면 mailbox는 지워지는데 기록은 남아, 다음 호출자가 "이미 구독됨" 빠른 경로를 타고 서버가 SUB를 받았는지 아무도 확인하지 않았다. 서버 도달 여부에 따라 유실이 갈리는 조건부 결함이다. 이제 flush가 끝난 뒤에 기록하고, 취소된 flush는 방금 만든 구독을 해지한다. 그룹 구독도 같다 (#11)

## [0.4.0] - 2026-09-09

### Added

- Python **3.14** 지원. CI 매트릭스에 3.14를 넣고 분류자를 추가했다. 로컬에서 CPython 3.14.7로 전체 테스트를 돌려 확인한 뒤 선언했다

### Changed

- `extensions`에서 **`flush`를 뺀다** (`["groups"]`). 스펙의 `flush` 확장은 분산 레이어가 모든 클라이언트에 비어 보이기를 요구하는데, 이 `flush()`는 한 인스턴스의 현재 이벤트 루프만 지운다. 메서드는 그대로 두고 선언만 실제에 맞춘다 — Channels 자체는 `extensions`를 읽지 않으므로 프레임워크 동작에는 영향이 없다 (#7)
- `group_expiry`를 **집행하지 않는다는 사실을 명시한다.** 값을 받아 두기만 하고 읽지 않는다는 것을 README와 모듈 docstring에 적었다. 동작은 그대로다 (#7)

### Fixed

- 같은 레이어 인스턴스를 **두 이벤트 루프**에서 수신하면 프로세스 전용 채널이 중복 전달됐다. 채널 이름의 프로세스 id가 인스턴스당 하나여서 두 루프가 같은 `<prefix>.pc.<process>` subject에 각각 구독을 만들고, 각자 자기 mailbox에 넣었다 — `send()` 한 번이 두 번 도착한다(재현함). id를 이벤트 루프별로 두어 루프마다 subject가 갈리고, 다른 루프의 채널로 `receive`·`group_add`를 부르면 기존 "다른 프로세스의 채널" 경로와 같이 `ValueError`가 된다. subject 형식은 그대로다 (#3)
- **프로세스 전용 채널의 mailbox 수명을 취소가 아니라 유휴 시간으로 판단한다.** 취소는 두 방향으로 어긋난다 — `wait_for`로 폴링하는 앱은 타임아웃마다 취소를 내지만 살아 있고(그래서 그룹 없는 채널을 폴링하면 폴링 사이의 메시지를 잃었다, #9), 채널 레이어 메시지를 받고 `StopConsumer`로 끝나는 컨슈머는 **취소할 대기 자체를 남기지 않는다**(Channels가 취소하는 태스크는 이미 완료된 것이라, 그 mailbox가 프로세스 수명 동안 남았다, #7). 이제 수신자도 트래픽도 그룹 소속도 없는 상태가 `mailbox_grace`(기본 60초) 지나면 회수한다. 일반 채널은 큐 그룹 몫을 가로채지 않도록 지금처럼 취소 즉시 놓아준다 (#9, #7)
- `group_add()`가 취소되거나 실패하면 **구독 없는 그룹 멤버십만 남았다**. 멤버 등록이 `subscribe_lock` 획득보다 먼저인데 되돌리는 코드가 없었다. 그 채널은 `group_send`를 받지 못하면서도, `_discard_mailbox()`에는 "그룹에 있으니 사용 중"으로 보여 mailbox가 프로세스 수명 동안 남았다. 실패하면 멤버십을 되돌린다 — 단, **그 채널로 배달할 구독이 이미 있으면 되돌리지 않는다.** 동시에 들어온 같은 `group_add`가 성공했다면 멤버십 항목을 공유하므로, 우리 쪽 기록이 아니라 구독의 존재가 판단 기준이다 (#12)
- 재접속 복구가 **그 사이 정리된 구독을 되살렸다**. `_resubscribe()`는 subscribe마다 await를 하는데, 그 틈에 실행된 `group_discard()`·`flush()`의 결과를 확인하지 않고 새 구독을 사전에 무조건 다시 써 넣었다. 실제 서버에서 `groups={}`인데 `group_subscriptions=['room']`이 되살아나고 서버에도 불필요한 구독이 남는 것을 확인했다. 이제 스냅샷으로 순회하고, 저장 전에 그 대상이 아직 필요한지 확인해 필요 없으면 방금 만든 구독을 해지한다. 같은 순회에서 mailbox 사전을 직접 돌다가 나던 `RuntimeError: dictionary changed size during iteration`도 함께 없앤다 (#14)
- 사용자가 `CONFIG`로 준 `closed_cb`가 예외를 던지거나 멈추면 레이어의 자동 복구가 **시작조차 되지 않았다**. `on_closed`가 사용자 콜백을 먼저 await한 뒤에 `_start_recovery()`를 불렀기 때문이다. 수신만 하는 프로세스는 `_client()`를 다시 부르지 않으므로 그대로 영영 못 받는다(실제 서버로 재현: 서버 재시작 후에도 `receive()` 미완료, 서버 쪽 구독 0개). 복구를 사용자 콜백보다 먼저 시작한다 (#13)
- `_mailbox()`가 구독이 붙기 전의 mailbox를 넘겼다. mailbox를 등록한 뒤에 구독하므로, 그 사이에 도착한 동시 호출자는 잠금 밖 빠른 경로에서 아직 아무것도 먹이지 않는 mailbox를 받았다. 준비 완료 신호를 두고 기다리게 한다 (#4)

## [0.3.2] - 2026-09-09

### Changed

- 역직렬화를 그룹 팬아웃 루프에서 `receive()`로 옮긴다. mailbox에는 인코딩된 바이트가 들어가고 수신자가 꺼낼 때 자기 것으로 파싱한다. 수신자별 격리는 그대로면서(각자 자기 바이트를 파싱), 용량·만료로 버려지는 메시지는 아예 디코드하지 않는다. 0.2.4가 도입한 멤버별 역직렬화로 10,000 멤버 p99가 12.2~13.4 ms까지 올랐던 것이 5.8~6.7 ms로 돌아온다
- README 벤치 절을 다시 쓴다. NATS의 p50과 InMemory의 값이 **서로 다른 것을 잰다**는 점, 프로세스 수가 8 대 1이라는 점, 이 숫자를 버전 간 비교로 읽으면 안 되는 이유를 밝힌다 (#8)
- `channels_redis`가 "그룹 멤버 수만큼 명령을 보낸다"는 서술을 고친다. 실제로는 발행자가 멤버 목록을 읽어 멤버 키마다 항목을 쓰되 연결별로 Lua 한 번에 묶는다. 없어지는 것은 **발행자 측 멤버 순회**이지 멤버별 작업 전부가 아니다 (#8)

## [0.3.1] - 2026-09-09

### Fixed

- 취소된 `receive()`를 무조건 연결 종료로 해석해 **그룹 멤버십을 지웠다**. `asyncio.wait_for(receive(ch), ...)`로 폴링하다 한 번만 타임아웃 나도 그 채널이 모든 그룹에서 빠져 이후 `group_send`가 도달하지 않았다(0.2.1의 취소 기반 정리가 만든 회귀). 그룹에 남아 있는 채널은 사용 중으로 보고 mailbox를 유지한다. 멤버십은 `group_discard`와 `flush`로만 빠진다
- 재접속 후 구독 복구가 실패하면 **열려 있지만 구독이 하나도 없는 연결**이 남았다. 이후 호출은 그 연결을 정상으로 보고 그대로 쓰고 `_recover`의 재시도 조건도 성립하지 않아, 해당 프로세스가 영구히 수신하지 못했다. 복구에 실패하면 연결을 닫아 다음 시도가 다시 붙게 한다
- 만료된 메시지가 대기열 자리를 계속 차지했다. 소비가 멈춘 사이 대기열이 차면 새 메시지가 버려지는데, 만료분은 `receive()`에서만 걷혀서 컨슈머가 재개해도 **아무것도 받지 못했다**(`capacity=1`, `expiry` 경과 상태에서 실측). 자리가 없을 때 만료분을 먼저 걷어낸다

### Changed

- mailbox 수명 서술을 정정한다. 그룹에 속한 채널은 취소되어도 유지되고, 그룹 없는 채널을 `wait_for`로 폴링하면 타임아웃 사이에 온 메시지를 놓친다 (#9)
- 순서 보장 범위를 **구독 하나 안**으로 정정해 README와 CLAUDE.md에 적는다. 직접 `send` 대 `group_send`뿐 아니라 서로 다른 두 그룹 사이에서도 역전된다. 와이어 순서는 옳다 — 수신 연결의 `Client._process_msg` 진입은 `[1, 2, 3]`인데 레이어의 `_enqueue` 진입이 `[1, 3, 2]`다. 뒤집는 것은 nats-py의 구독별 대기열·태스크다. 다만 `[1, 3, 2]`는 재현 결과이지 고정된 결과가 아니고, 보장되지 않는다는 것이 요지다. 발행자 측 그룹 펼치기가 필요하다는 이전 설명은 틀렸다 — 수신 측 단일 디스패처로 고칠 수 있고, 그것을 지금 감당하지 않기로 한 것이다

## [0.3.0] - 2026-09-09

subject 형식은 그대로지만 **본문 인코딩과 일반 채널의 그룹 구독 방식이 바뀐다.** 0.2.x와 0.3.0 프로세스를 한 NATS에 섞으면 서로의 메시지를 읽지 못한다. 전부 함께 올린다.

### Changed

- 본문 직렬화를 **msgpack으로 고정**하고 `serializer` 옵션과 `[msgpack]` extra를 없앤다. Channels 메시지는 바이트열을 실을 수 있는데 기본이던 JSON은 그것을 표현하지 못했다(스펙 위반). 게다가 직렬화 형식은 모든 프로세스가 같아야 하는 와이어 계약이라, 한 곳만 다르게 설정하면 조용히 디코드에 실패한다 — 검증할 수 없는데 반드시 일치해야 하는 값은 옵션이 아니다. `msgpack`은 이제 고정 의존성이다(`channels_redis`도 같다)
- 그룹의 일반 채널 멤버는 그룹 subject를 **그 채널의 큐 그룹으로** 구독한다. 전에는 같은 채널 이름을 읽는 프로세스 전원이 `group_send` 사본을 받았다. 0.2.2의 큐 그룹 수정이 직접 `send` 경로만 덮었다

### Added

- `MessageTooLarge`와 `ChannelFull`을 레이어 속성으로 노출한다(스펙 요구). `ChannelFull`은 여전히 발생하지 않는다 — pub/sub에서 보내는 쪽은 받는 쪽 대기열을 볼 수 없다

### Fixed

- 본문이 `max_payload` 이내여도 `Channel` 헤더가 더해져 프레임이 한도를 넘으면 **서버가 연결을 끊었다**. 그 연결의 다른 채널까지 함께 죽었다. nats-py는 본문만 검사하므로 헤더까지 합산해 미리 `MessageTooLarge`를 던진다

## [0.2.4] - 2026-09-09

### Fixed

- 동시에 도착한 컨슈머들이 프로세스 구독을 각자 만들었다. `new_channel()` 8개를 동시에 부르면 실제 구독이 8개 생겨 `send` 한 번이 8번 전달됐다(추적은 1개라 `flush()`도 초과분을 정리 못 했다). 구독 생성을 루프별 잠금으로 묶는다. 연결이 몰리는 실서버에서 바로 나던 at-most-once 위반이다
- `channel_capacity`를 설정하면 수신이 깨졌다. `get_capacity()`는 컴파일된 (패턴, 용량) 쌍을 순회하는데 `BaseChannelLayer`는 원본 dict를 그대로 두므로 레이어가 직접 `compile_capacities()`를 불러야 한다
- 수신만 하는 프로세스가 재접속 뒤에도 죽어 있었다. 0.2.1의 복구는 `_client()`를 부를 때만 도는데 `receive()`는 그것을 부르지 않는다. 연결이 닫히면 백오프를 두고 스스로 재접속해 구독을 복구한다
- 그룹 수신자들이 역직렬화된 **같은 dict 객체**를 받았다. 한 컨슈머가 고치면 다른 컨슈머가 받은 내용도 바뀌었다. 멤버마다 자기 객체를 받는다

## [0.2.3] - 2026-09-09

### Changed

- `close()`도 닫힌 다른 루프의 상태를 함께 정리한다. 이전에는 새 루프가 등록될 때만 스윕이 돌아서 마지막 항목이 남았다
- 이벤트 루프 수가 `loop_state_warn_at`(기본 32)을 넘으면 한 번 경고한다. `close()` 없이 버려진 루프는 살아 있는 루프와 구분할 수 없어 연결이 계속 잡히는데, 이전에는 그것이 조용히 자랐다

## [0.2.2] - 2026-09-09

### Fixed

- `!`가 없는 일반 채널을 여러 프로세스가 읽으면 `send` 하나가 전원에게 복제됐다. 같은 채널 이름으로 워커를 N개 띄우면 모든 워커가 모든 작업을 실행했다. 이제 subject와 같은 이름의 큐 그룹으로 구독해 그중 하나만 받는다 (#2). `.ch.` subject에 합류하는 외부 워커도 같은 큐 그룹을 써야 한다

## [0.2.1] - 2026-09-09

### Added

- `scripts/release_check.py`와 `make release-check`. 태그·CHANGELOG 절·License 분류자가 `pyproject.toml`의 version과 맞는지 본다. release.yml이 태그를 받아 같은 스크립트를 돌리므로 로컬과 CI가 같은 검사를 쓴다
- 3노드 클러스터를 띄우는 `nats_cluster` fixture와 `tests/test_cluster.py`. 노드 간 group/채널 라우팅과, 워커가 붙어 있던 노드를 죽였을 때의 페일오버를 검증한다. 구독 관심사가 route를 타고 다른 노드까지 가는 데 걸리는 시간은 README 클러스터 절에 적었다
- 벤치 결과 JSON과 출력에 무엇을 쟀는지 남긴다(`measured`: 레이어 버전, `git describe --dirty`, Python, nats-server). 파일만 보고 어느 버전의 수치인지 알 수 있고, 파일을 바꿔치기한 A/B는 `-dirty`로 드러난다

### Changed

- mailbox가 가득 차 메시지를 버릴 때 메시지마다 찍던 경고를, 첫 드롭 한 번과 이후 60초당 한 번(`drop_log_interval`)으로 줄인다. 자리가 나면 그동안 버린 개수를 한 줄로 남긴다. 가득 찬 상태는 보통 지속되므로 이전에는 과부하 시 로그가 폭주했다

### Fixed

- nats-py가 재접속을 포기하고 클라이언트를 닫으면(기본 `max_reconnect_attempts=60` 소진) 그 뒤 새로 연 연결에 구독이 하나도 없어서, 프로세스가 발행은 계속하고 수신은 영원히 못 하는 무음 상태가 됐다. 이제 재접속할 때 해당 루프의 프로세스·채널·그룹 구독을 모두 복구한다
- 컨슈머가 끊긴 뒤에도 mailbox가 남아, 메모리가 동시 연결 수가 아니라 누적 연결 수에 비례해 자랐다. 마지막 `receive()`가 취소되면 mailbox와 일반 채널 구독, 남은 그룹 멤버십을 정리한다
- 다른 프로세스가 만든 프로세스 전용 채널로 `receive`/`group_add`를 부르면 그 프로세스의 `pc` subject를 구독해 버려서, 해당 채널 메시지가 양쪽에 중복 전달됐다. 이제 `ValueError`로 막는다. 그 채널로 `send`하는 것은 그대로 동작한다
- `flush()`가 이미 끊긴 연결에서 `ConnectionClosedError`로 터졌다. 구독 해제 실패를 `_discard_mailbox`와 같은 방식으로 흘려보내고, 상태는 첫 await 전에 비운다. NATS가 죽은 상태의 종료가 에러가 되지 않는다
- 이벤트 루프별 상태(`_states`)가 루프가 닫힌 뒤에도 남아 있었다. 새 루프가 등록될 때 닫힌 루프의 상태를 쓸어낸다. 루프를 만들고 버리는 코드에서 죽은 루프와 연결이 쌓이지 않는다
- `channels` 하한을 4.2.1로 올린다. 레이어가 쓰는 `require_valid_channel_name`/`require_valid_group_name`이 4.2.1부터 있어서, 4.0~4.2.0에서는 첫 `send()`가 AttributeError로 죽었다
- `channels_nats.__version__`이 `"0.2.0"`에 멈춰 있었다. 이제 설치 메타데이터에서 읽어 `pyproject.toml`과 어긋날 수 없다
- MIT LICENSE 파일을 추가하고 `license-files`로 sdist·wheel에 싣는다. 0.2.0 배포물에는 라이선스 원문이 없었다
- `License :: OSI Approved :: MIT License` 분류자를 뺀다. PEP 639에서 `License-Expression`과 함께 쓰는 것이 금지됐다. `scripts/release_check.py`가 재발을 막는다 (`twine check`는 잡지 못한다)

## [0.2.0] - 2026-09-08

### Changed

- 프로세스 전용 채널(`specific.<process>!<id>`)은 채널마다 구독하지 않고 프로세스당 구독 하나(`<prefix>.pc.<process>`)로 받는다. 전체 채널 이름은 NATS 헤더 `Channel`로 전달된다. 컨슈머 연결 하나의 비용이 NATS 구독과 콜백 태스크에서 로컬 대기열 하나로 준다 (#1)
- `channel_subject()`가 `!`가 있는 이름에 대해 `pc` subject를 돌려준다. `ch` subject는 일반 채널 전용

## [0.1.0] - 2026-09-08

- 첫 버전. `send`/`receive`/`new_channel`/`group_*`/`flush`, json과 msgpack, 루프별 연결, Ubuntu·Windows CI, fan-out 벤치
